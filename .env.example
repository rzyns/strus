# Copy to .env and adjust as needed

# Absolute path to the SQLite database file
# Using an absolute path avoids relative-path issues when running from different package dirs
STRUS_DB_PATH=/home/you/dev/strus/strus.db

# API server port (default: 3457 — avoids WSL2 Windows-forwarded ports like 3000)
PORT=3457
