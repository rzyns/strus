import { css } from '../../../styled-system/css'

export default function LemmaDetail() {
  return <div class={css({ py: '4' })}>
    <h1 class={css({ fontSize: '2xl', fontWeight: 'bold' })}>Lemma Detail</h1>
    <p class={css({ color: 'fg.muted' })}>Coming soon.</p>
  </div>
}
